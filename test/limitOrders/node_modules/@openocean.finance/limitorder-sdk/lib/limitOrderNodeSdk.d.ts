import { ChainId } from "./utils/limit-order-sdk";
import Web3 from "web3";
import { JsonRpcProvider, Wallet } from "ethers6";
import { Account } from 'web3-core';
type Provider = Web3 | JsonRpcProvider;
declare class LimitOrderNodeSdk {
    private readonly chainId;
    readonly provider: Provider;
    private readonly ethersSigner;
    private readonly mode?;
    private readonly limitOrderContractAddress;
    private readonly chain;
    private readonly builder;
    private readonly orderFacade;
    private readonly isEthers;
    private readonly ethersProvider?;
    private readonly signerAddress;
    constructor(chainId: ChainId, provider: Provider, ethersSigner: Wallet | Account, mode?: string | undefined);
    private getLimitOrderBuilder;
    private getProviderConnector;
    private getLimitOrderFacade;
    private getLimitContractAddress;
    createLimitOrder(orderParams: {
        makerTokenAddress: string;
        makerTokenDecimals: number;
        takerTokenAddress: string;
        takerTokenDecimals: number;
        makerAmount: string;
        takerAmount: string;
        gasPrice: number;
        expire: string;
        receiver?: string;
        receiverInputData?: string;
        mode?: string;
    }): Promise<{
        makerAmount: string;
        takerAmount: string;
        signature: string;
        orderHash: string;
        orderMaker: string;
        remainingMakerAmount: string;
        data: import("./utils/limit-order-sdk").EIP712Object;
        isActive: boolean;
        chainId: any;
        expireTime: number;
        amountRate: string;
        interaction: string;
    }>;
    cancelLimitOrder(orderParams: {
        gasPrice: any;
        orderData: any;
    }): Promise<unknown>;
    sendEthApprove(inToken: string, type: string, amount: string, gasPrice: number, toContract: string): Promise<any>;
    loadChart(option: any): Promise<void>;
}
export default LimitOrderNodeSdk;
