name: test

on:
  - pull_request

env:
  FOUNDRY_PROFILE: ci
  CONFIGS: |
    certora/confs/setup/autoCVL_AccountantWithRateProviders.conf
    certora/confs/setup/autoCVL_AccountantWithYieldStreaming.conf
    certora/confs/setup/autoCVL_BoringVault.conf
    certora/confs/setup/autoCVL_TellerWithMultiAssetSupport.conf
    certora/confs/setup/sanity_AccountantWithRateProviders.conf
    certora/confs/setup/sanity_AccountantWithYieldStreaming.conf
    certora/confs/setup/sanity_BoringVault.conf
    certora/confs/setup/sanity_GenericRateProvider.conf
    certora/confs/setup/sanity_GenericRateProviderWithDecimalScaling.conf
    certora/confs/setup/sanity_LayerZeroTeller.conf
    certora/confs/setup/sanity_TellerWithBuffer.conf
    certora/confs/setup/sanity_TellerWithMultiAssetSupport.conf
    certora/confs/setup/scenarioA.conf
    certora/confs/setup/scenarioB.conf
    certora/confs/setup/scenarioC.conf
    certora/confs/setup/scenarioD.conf
    certora/confs/setup/scenarioE.conf

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      statuses: write
      pull-requests: write
      id-token: write
    steps:
      - name: checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: yarn install
        run: |
          corepack enable
          yarn install

      - name: run configs
        uses: Certora/certora-run-action@v2
        with:
          configurations: ${{ env.CONFIGS }}
          solc-versions: 0.8.21
          solc-remove-version-prefix: "0."
          job-name: "Verified Rules"
          certora-key: ${{ secrets.CERTORAKEY }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
